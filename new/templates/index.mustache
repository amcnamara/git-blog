<html>
  {{> ./partials/_meta_header}}
  <body>
    <div class="container">
      {{> ./partials/_content_header}}
      <div class="index">
        {{#index}}
          <div class="post">
            <div class="title">{{title}}</div>
            <div class="description">{{description}}</div>
          </div>
        {{/index}}
      </div>
      {{> ./partials/_content_footer}}
    </div>
  </body>
</html>
